<!DOCTYPE html>
<html lang="en">
  <!-- Navbar -->
  <%- include('layout/head') %>
  <body class="bg-gray-900 text-white font-sans">
    <!-- Navbar -->
    <%- include('layout/nav') %>

    <!-- Hero / Page Title -->
    <section
      class="relative flex items-center justify-center min-h-screen text-center bg-gray-900 overflow-hidden"
    >
      <!-- Glow Background -->
      <div
        class="absolute -top-32 -left-32 w-96 h-96 bg-yellow-400 rounded-full mix-blend-screen blur-3xl opacity-20 animate-pulse"
      ></div>
      <div
        class="absolute -bottom-32 -right-32 w-96 h-96 bg-purple-500 rounded-full mix-blend-screen blur-3xl opacity-20 animate-pulse"
      ></div>

      <div class="relative z-10 max-w-3xl mx-auto px-4">
        <h1
          class="text-4xl sm:text-5xl md:text-6xl font-extrabold text-white mb-4"
        >
          Upgrade Your Server
          <span class="text-yellow-400">to the Next Level</span>
        </h1>
        <p class="text-gray-300 max-w-2xl mx-auto text-lg sm:text-xl mb-8">
          Unlock powerful features, automate your server, and boost engagement
          with <span class="text-blue-400 font-semibold">Beetle Premium</span>.
        </p>

        <div class="flex flex-col sm:flex-row justify-center gap-4">
          <!-- Primary CTA -->
          <a
            href="https://www.patreon.com/c/beetlebot/membership"
            class="bg-yellow-400 hover:bg-yellow-500 text-gray-900 font-bold py-3 px-8 sm:py-4 sm:px-10 rounded-2xl shadow-lg transition transform hover:scale-105 flex items-center justify-center gap-2"
          >
            Unlock Now
          </a>

          <!-- Secondary CTA -->
          <a
            href="#pricing"
            class="bg-transparent border border-gray-500 text-gray-300 hover:text-white hover:border-white font-semibold py-3 px-8 sm:py-4 sm:px-10 rounded-2xl shadow-lg transition transform hover:scale-105 flex items-center justify-center gap-2"
          >
            See Pricing
          </a>
        </div>
      </div>
    </section>

    <!-- Pricing Tables -->
    <section
      id="pricing"
      class="max-w-6xl mx-auto px-4 sm:px-6 py-16 grid md:grid-cols-2 gap-12 scroll-mt-24"
    >
      <!-- Free Plan -->
      <div
        class="bg-gray-800 rounded-2xl shadow-xl p-8 flex flex-col items-center"
      >
        <h2 class="text-2xl font-bold text-white mb-2">Free</h2>
        <p class="text-gray-300 mb-6 text-center">
          Enjoy basic plugins for small communities
        </p>
        <span class="text-4xl font-extrabold text-blue-400 mb-6">
          $0<span class="text-lg">/month</span>
        </span>
        <a
          href="https://discord.com/oauth2/authorize?client_id=1308317199022161981&permissions=8&integration_type=0&scope=bot+applications.commands"
          class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-2xl transition"
          >Get Free</a
        >
      </div>

      <!-- Premium Plan -->
      <div
        class="relative bg-gray-800 rounded-2xl shadow-xl p-8 flex flex-col items-center overflow-hidden border-2 border-yellow-400"
      >
        <!-- Gold shine effect -->
        <div
          class="absolute inset-0 bg-gradient-to-r from-yellow-400 via-white to-yellow-400 opacity-20 animate-shine pointer-events-none"
        ></div>

        <h2 class="text-2xl font-bold text-white mb-2 z-10 relative">
          Premium
        </h2>
        <p class="text-gray-300 mb-6 text-center z-10 relative">
          Access to unlimited plugins
        </p>
        <span class="text-4xl font-extrabold text-yellow-400 mb-6 z-10 relative"
          >$7.99<span class="text-lg">/month</span></span
        >
        <a
          href="https://www.patreon.com/c/beetlebot/membership"
          class="bg-yellow-400 hover:bg-yellow-500 text-gray-900 font-bold py-3 px-8 rounded-2xl transition z-10 relative"
          >Subscribe Now</a
        >
      </div>
    </section>

    <style>
      @keyframes shine {
        0% {
          transform: translateX(-100%) rotate(25deg);
        }
        100% {
          transform: translateX(200%) rotate(25deg);
        }
      }

      .animate-shine {
        position: absolute;
        top: 0;
        left: -50%;
        width: 150%;
        height: 100%;
        background: linear-gradient(
          120deg,
          rgba(255, 255, 255, 0) 0%,
          rgba(255, 255, 255, 0.4) 50%,
          rgba(255, 255, 255, 0) 100%
        );
        animation: shine 3s infinite;
      }
      html {
        scroll-behavior: smooth;
      }
    </style>

    <!-- Benefits Section -->
    <section class="max-w-6xl mx-auto px-4 sm:px-6 py-16">
      <h2
        class="text-3xl sm:text-4xl font-bold text-blue-400 text-center mb-12"
      >
        Why Choose Beetle?
      </h2>
      <div class="grid md:grid-cols-3 gap-8 text-gray-300">
        <div class="flex flex-col items-center">
          <i class="fas fa-bolt text-4xl text-blue-400 mb-4"></i>
          <h3 class="font-bold text-white mb-2">Fast & Reliable</h3>
          <p class="text-center">
            Commands and automation run instantly with minimal delay.
          </p>
        </div>
        <div class="flex flex-col items-center">
          <i class="fas fa-cogs text-4xl text-blue-400 mb-4"></i>
          <h3 class="font-bold text-white mb-2">Powerful Tools</h3>
          <p class="text-center">
            Moderation, economy, games, events, and automation in one bot.
          </p>
        </div>
        <div class="flex flex-col items-center">
          <i class="fas fa-users text-4xl text-blue-400 mb-4"></i>
          <h3 class="font-bold text-white mb-2">Boost Engagement</h3>
          <p class="text-center">
            Keep your server lively with activities, contests, and interactive
            commands.
          </p>
        </div>
      </div>
    </section>

    <%- include('layout/footer') %>
  </body>
</html>
